@model ICollection<CRUD_Operations_with_DB_Context.Models.Supplier>
@{
    ViewBag.Title = "DisplayAll";
}

    <div class="container">
        <div style="height:30px">
        </div>
        @{
            Int16 counter = 1;

            System.Reflection.MethodInfo[] methods = typeof(HtmlHelper).GetMethods();
            HashSet<string> listOfNames = new HashSet<string>();
            foreach (var method in methods)
            { listOfNames.Add(method.Name); }

            @*<table class="table">
                <tr>
                    <th>NO</th>
                    <th>Method</th>
                </tr>

                @foreach (var name in listOfNames)
                {
            <tr>
                <td>@counter</td>
                @{ counter++;}

                <td>@name</td>
            </tr>
                }

            </table>*@
        }
        <div style="height:30px">

        </div>
        <table class="table table-striped table-bordered border-dark text-center">
            <tr class="text-center">
                <th class="text-center">Supplier ID</th>
                <th class="text-center">Supplier Name</th>
                <th class="text-center">City</th>
                <th class="text-center">Modify</th>
                <th class="text-center">Remove</th>
            </tr>

            @foreach (var item in Model)
            {
                <tr>
                    <td>@item.SupID</td>
                    <td>@item.Name</td>
                    <td>@item.City</td>
                    <td><a href="/Supplier/Update/@item.SupID" class="btn btn-default">Modify</a></td>
                    <td><a href="/Supplier/Delete/@item.SupID" class="btn btn-danger" @*data-toggle="modal" data-target="#staticBackdrop"*@>Remove</a></td>
                </tr>

            }
        </table>
        <center>
            <a class="btn btn-success" href="/Supplier/insert">Add New Supplier</a>
        </center>
        @*<div class="modal fade" id="staticBackdrop" data-backdrop="static" tabindex="-1" role="dialog" aria-labelledby="staticBackdropLabel" aria-hidden="true">
            <div class="modal-dialog" role="document">
                <div class="modal-content">
                    <div class="modal-header">
                        <h5 class="modal-title" id="staticBackdropLabel">Modal title</h5>
                        <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                            <span aria-hidden="true">&times;</span>
                        </button>
                    </div>
                    <div class="modal-body">
                        ...
                    </div>
                    <div class="modal-footer">
                        <button type="button" class="btn btn-secondary" data-dismiss="modal">Close</button>
                        <button type="button" class="btn btn-primary">Understood</button>
                    </div>
                </div>
            </div>
        </div>*@
        <div style="height:30px">

        </div>

    </div>

@*@section scripts{
       <script>
        $( function() {
            $("body").css("background-color", "red");
  } );
    </script>
}*@


